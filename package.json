{
  "name": "demoqa-api-playwright-generator",
  "version": "2.0.0",
  "private": true,
  "description": "Spec-driven API test generation and execution using OpenAPI/Swagger and Playwright.",
  "scripts": {
    "generate": "node generator/index.js",
    "test": "playwright test",
    "api:test": "npm run generate && playwright test",
    "report": "playwright show-report",
    "generate:test": "npm run generate"
  },
  "devDependencies": {
    "@apidevtools/swagger-parser": "^12.0.0",
    "@playwright/test": "^1.62.1",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0"
  }
}
